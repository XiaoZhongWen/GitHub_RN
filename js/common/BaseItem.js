import React, {Component} from 'react';

export default class BaseItem extends Component {
    constructor(props) {
        super(props);
        // this.state = {
        //     isFavorite: false,
        // };
    }
}
